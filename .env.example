# Environment Variables Example

# API Configuration
# API_BASE_URL=http://localhost:3000/api

# Database (if migrating to PostgreSQL in the future)
# DATABASE_URL=postgresql://user:password@localhost:5432/invoice_db

# Authentication (if adding auth later)
# JWT_SECRET=your-secret-key-here
# API_KEYS=key1,key2,key3

# Deployment (if using cloud services)
# NUXT_PUBLIC_API_BASE_URL=https://your-domain.com/api
